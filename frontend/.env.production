# Overridden at Docker build time via --build-arg VITE_API_URL=... (see frontend/Dockerfile).
# This value is only used as a fallback for local `vite build` runs without Docker.
VITE_API_URL=http://localhost:5000/api
